@page "/{**Route:nonfile}"

<CascadingValue Value="UserLogin">
    <PageTitle>@Page?.Title</PageTitle>
    <HeadContent>
        @if (Page?.Layout?.Head != null)
        {
            @((MarkupString)Page.Layout.Head)
        }
    </HeadContent>
    @ChildComponents()
</CascadingValue>

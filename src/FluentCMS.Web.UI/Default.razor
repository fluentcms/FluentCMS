@page "/{**Route:nonfile}"

<CascadingValue Value="UserLogin">
    <PageTitle>@Page?.Title</PageTitle>
    <HeadContent>
        @if (Page?.Layout?.Head != null)
        {
            @((MarkupString)GetParsedContent(Page.Layout.Head))
        }
    </HeadContent>
    @ChildComponents()
</CascadingValue>

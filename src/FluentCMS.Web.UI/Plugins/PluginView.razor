<Card>
    @if (!string.IsNullOrEmpty(Title))
    {
        <CardTitle>@Title</CardTitle>
    }
    @if (ChildContent != null)
    {
        <CardBody>
            @ChildContent
        </CardBody>
    }
</Card>

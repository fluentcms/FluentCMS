@page "/{*Route:nonfile}"
@inherits BasePage

<PageTitle>@AppState.Page?.Title</PageTitle>
<HeadContent>
    @if (AppState.Layout != null)
    {
        @((MarkupString)AppState.Layout.Head)
    }
</HeadContent>
@dynamicComponent()

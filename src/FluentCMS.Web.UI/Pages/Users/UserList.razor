@page "/users"
@inject HttpClient httpClient

<h3>Users</h3>
<p>List of all users.</p>
<NavigationButton To="/users/create">Create</NavigationButton>

@if (users == null)
{
    <p>Loading...</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Username</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in users)
            {
                <tr>
                    <td>@user.Name</td>
                    <td>@user.Username</td>
                    <td>
                        <NavigationButton To="@("/users/edit/" + user.Id)">Edit</NavigationButton>
                        <Button @onclick="() => OnDeleteClick(user.Id)">Delete</Button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private List<Entities.User>? users;

    private async Task LoadUsers()
    {
        try
        {
            var response = await httpClient.GetFromJsonAsync<ApiResult<IEnumerable<Entities.User>>>("users");
            if (response != null && response.IsSuccess)
            {
                users = response.Data!.ToList();
            }
        }
        catch (Exception ex)
        {
            throw;
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadUsers();
    }

    private async Task OnDeleteClick(Guid id)
    {
        try
        {
            var response = await httpClient.DeleteFromJsonAsync<ApiResult<bool>>("users/" + id);
            if (response != null && response.IsSuccess)
            {
                await LoadUsers();
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            throw;
        }
    }
}

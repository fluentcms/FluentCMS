@typeparam TValue

@inherits NumberInput<TValue>

@namespace FluentCMS.Web.UI.Plugins.Components

@if (Visible)
{
    <PluginFormField Cols="Cols">
        @this.RenderBase()
    </PluginFormField>
}

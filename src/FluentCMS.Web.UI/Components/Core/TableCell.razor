@inherits BaseComponent

<th class=@("f-table-cell" + (SmallWidth ? " f-table-cell-small" : "") + Class) @attributes=@RestProps>
    @ChildContent
</th>

@code {

    [Parameter]
    public bool SmallWidth { get; set; }
}
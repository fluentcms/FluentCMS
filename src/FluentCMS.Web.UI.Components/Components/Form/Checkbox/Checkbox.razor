@inherits FormCheckboxesType<bool>

@namespace FluentCMS.Web.UI.Components

@if(Visible)
{
    <FormField2 Appearance="FormFieldAppearance.Checkbox"
               Field="this">
        <InputCheckbox @attributes="AdditionalAttributes"
                       @bind-Value="Value"
                       class="@this.GetClasses()"
                       disabled="@Disabled"
                       id="@Id"
                       name="@Name" />
    </FormField2>
}

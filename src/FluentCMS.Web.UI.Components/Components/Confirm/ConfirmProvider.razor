@namespace FluentCMS.Web.UI.Components

@if (Open)
{
    <Confirm @bind-Open="Open"
             Message="@Message"
             Static="true"
             OnCancel="OnCancel"
             OnConfirm="OnConfirm" />
}

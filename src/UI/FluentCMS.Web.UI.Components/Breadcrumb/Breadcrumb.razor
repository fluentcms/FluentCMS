@inherits BaseComponent

@namespace FluentCMS.Web.UI.Components

@if (Visible)
{
    <nav @attributes="AdditionalAttributes" class="@this.GetClasses()">
        <ol class="@this.ClassName("BreadcrumbInner")">
            @ChildContent
        </ol>
    </nav>
}
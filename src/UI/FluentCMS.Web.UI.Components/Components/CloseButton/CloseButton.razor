@inherits BaseComponent

@namespace FluentCMS.Web.UI.Components

<button @attributes="AdditionalAttributes" class="@this.GetClasses()" type="button">
    <Icon Name="IconName.Close" Size="IconSize.Large" />
</button>

@code {
    protected override RenderFragment BuildContent => (__builder => { });
}


{
    "name": "fluentcms.e2e",
    "scripts": {
        "test:open": "cypress open",
        "test:open:mobile": "CYPRESS_MOBILE=true npm run test:open",
        "test:mobile": "CYPRESS_MOBILE=true npm run test",
        "test": "cypress run",
        "test:both": "rm -r cypress/reports/desktop; rm -r cypress/reports/mobile; npm run test && mv cypress/reports/html cypress/reports/desktop && npm run test:mobile && mv cypress/reports/html cypress/reports/mobile"
    },
    "devDependencies": {
        "cypress-mochawesome-reporter": "^3.8.2",
        "cypress": "^13.6.4"
    }
}

<div class="flex flex-wrap gap-4 mt-2 text-content-muted">
    {{ for skill in Items }}
        <span class="px-3 py-1 bg-primary-100 text-primary-700 rounded-full">{{ skill.skill_name }} - {{ skill.proficiency }}</span>
    {{ end }}
</div>
@namespace FluentCMS.Web.Plugins.Admin.ContentTypeManagement

@if (_value != null)
{
    @if (string.IsNullOrEmpty(FieldModel?.Format))
    {
        @_value?.ToString()
    }
    else
    {
        @_value?.ToString(FieldModel.Format)
    }
}

@inherits BaseContentTypeFieldComponent

@namespace FluentCMS.Web.Plugins.Admin.ContentTypeManagement

@if (Visible && ContentTypeField != null && ContentType != null)
{
    <StringFieldSettings Model=@(ContentTypeField.ToFieldModel<StringFieldModel>()) OnCancel=OnComplete OnSubmit=OnFieldUpdate />
}

@inherits BaseContentTypeFieldComponent

@namespace FluentCMS.Web.Plugins.Admin.ContentTypeManagement

@if (Visible)
{
    @if (string.IsNullOrEmpty(ContentTypeField.Type))
    {
        <ContentTypeFieldSelector OnSelect=OnTypeSelect OnCancel=OnComplete />
    }
    else
    {
        <DynamicComponent Type="FieldTypes.GetSettingType(ContentTypeField.Type)" Parameters="GetParameters()" />
    }
}

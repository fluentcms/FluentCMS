@inherits BasePlugin

<PluginForm Model="@Model" Name="@FORM_NAME" Title="Update Site" OnSubmit="OnSubmit">
    <FormInput @bind-Value="Model!.Name" Label="Name" Placeholder="Enter Name" Required />
    <FormInput Hint="Comma separated Urls without http(s)" @bind-Value="Model!.Urls" Cols="6" Label="Urls" Placeholder="Enter each url in new line" />
    <FormSelect Hint="Page's Default Layout will replace this value." @bind-Value="Model!.LayoutId" Data="Layouts" ValueField="Id" TextField="Name" Cols="6" Label="Default Layout" Placeholder="Choose Layout" Visible="@(Layouts != null)" />
    <FormSelect Hint="Page's EditView Layout will replace this value." @bind-Value="Model!.EditLayoutId" Data="Layouts" ValueField="Id" TextField="Name" Cols="6" Label="EditView Layout" Placeholder="Choose Layout" Visible="@(Layouts != null)" />
    <FormSelect Hint="Page's SingleView Layout will replace this value." @bind-Value="Model!.DetailLayoutId" Data="Layouts" ValueField="Id" TextField="Name" Cols="6" Label="SingleView Layout" Placeholder="Choose Layout" Visible="@(Layouts != null)" />
    <FormTextarea @bind-Value="Model!.Description" Label="Description" Placeholder="Enter Description" />
    <FormInput Hint="Page's MetaTitle will replace this value." @bind-Value="Model!.MetaTitle" Cols="12" Label="Meta Title" Placeholder="Enter Meta Title" />
    <FormInput Hint="Page's MetaKeywords will replace this value." @bind-Value="Model!.MetaKeywords" Cols="12" Label="Meta Keywords" Placeholder="Enter Meta Keywords" />
    <FormTextarea Hint="Page's MetaDescription will replace this value." @bind-Value="Model.MetaDescription" Label="Meta Description" Placeholder="Enter Meta Description" />
    <FormActions Cancel Submit />
</PluginForm>

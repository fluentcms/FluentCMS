@page "/{**Route:nonfile}"

<PageTitle>@ViewContext.Page?.Title</PageTitle>
<HeadContent>
    @RenderDynamicContent(ViewContext.Page?.Layout?.Head ?? string.Empty)
</HeadContent>
@RenderDynamicContent(ViewContext.Page?.Layout?.Body ?? string.Empty)
